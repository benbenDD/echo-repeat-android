# 回声复读 v2.0.12

## 修复锁屏跟读再次延迟

- 真机日志确认 09:30:09 开始的一次 4 秒跟读间隔直到 09:32:10 才结束，实际延误 116.708 秒，并在手机亮屏后恢复。
- 故障期间播放服务没有销毁、没有播放器错误、也没有收到暂停命令；被延迟的是普通主线程计时回调。
- 跟读静音现在通过播放器静音重放当前片段来完成，使用媒体时间轴决定结束，不再依赖锁屏时可能被小米系统冻结的普通延时任务。
- 静音阶段继续维持媒体播放管线，片段结束后恢复正常音量并进入下一遍或下一段。
- 跟读暂停、继续、倍速换算和字幕时间轴改为读取静音播放的真实媒体位置。
- 普通分段间隔仍使用原有短时计时逻辑。

## 调整播放诊断日志

- 两个轮换文件从每个 256 KB 提升到每个 1 MB，总上限约 2 MB。
- 不再写入没有动作含义的 `command action=null`。
- 通知状态只在内容发生变化时记录，减少重复日志。
- 保留跟读开始与结束、恢复出声、暂停来源、服务生命周期、播放错误和睡眠定时等关键事件。

## 构建验证

- versionName：2.0.12
- versionCode：32
- Kotlin/Compose 编译成功。
- Debug APK 构建成功。
